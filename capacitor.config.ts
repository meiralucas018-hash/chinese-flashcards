import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.neon.chineseflashcards', // Altere conforme necessário
  appName: 'Chinese Flashcards',
  webDir: 'out',
  bundledWebRuntime: false,
};

export default config;
